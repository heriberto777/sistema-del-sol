{
  "name": "agente-caja",
  "version": "1.0.0",
  "description": "Agente local para abrir la gaveta de dinero (impresora térmica USB con cable de gaveta, o puerto serial/COM directo) desde el POS de Sistema del Sol. Standalone, fuera del workspace pnpm — se distribuye como .exe aparte.",
  "private": true,
  "main": "src/servidor.js",
  "scripts": {
    "start": "node src/servidor.js",
    "build": "pkg . --targets node18-win-x64 --output dist/agente-caja.exe"
  },
  "dependencies": {
    "serialport": "^12.0.0"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^5.11.0"
  },
  "pkg": {
    "assets": ["config.json"]
  }
}
